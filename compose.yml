services:
  backend:
    image: noterian:latest
    restart: always
    expose:
      - '8000'

  frontend:
    build: .
    restart: always
    ports:
      - '80:80'
    depends_on:
      - backend
