<ul class="validator__list">
    {{#each requirements}}
        <li class="validator__requirement">
            {{{svgIcon (ternary isMet "req_yes" "req_no") class="icon" alt="Requirement status"}}}
            {{label}}
        </li>
    {{/each}}
</ul>