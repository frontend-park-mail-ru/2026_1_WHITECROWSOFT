<button
    type="{{type}}"
    class="btn {{#if variant}}btn--{{variant}}{{/if}} {{className}}"
    {{#if loading}}data-loading{{/if}}
    disabled
    style="cursor: not-allowed; opacity: 0.6;"
>
    {{text}}
</button>
